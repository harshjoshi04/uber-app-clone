import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text className={""}>Rides</Text>
    </SafeAreaView>
  );
};

export default Rides;
